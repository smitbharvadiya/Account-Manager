# 🔐 React User Authentication App

A modern and minimal **User Authentication System** built with **React** and **Tailwind CSS**.  
This app allows users to **register, login, view account info, update their details, and logout** — all using **localStorage** for session management.  

---

## 🧭 Overview

This project is a React application (V16+) that allows users to create and manage accounts. The application includes a login page, a registration page, and a page where users can view and edit their account information.

---

## ✨ Features

✅ Register new users with username and email  
✅ Login authentication (validates stored credentials)  
✅ Account dashboard showing username & email  
✅ Update user info (email/username)  
✅ Logout clears localStorage and session  
✅ Redirects unauthorized users to Login  
✅ Beautiful and responsive Tailwind UI  

---

## ⚙️ Tech Stack

- **Frontend:** React.js (V16+)
- **Styling:** Tailwind CSS
- **Routing:** React Router v6
- **State Management:** React Hooks (`useState`, `useEffect`)
- **Storage:** localStorage API

---

## 📂 Folder Structure

- **public/** — Contains the HTML template and static assets.  
- **src/** — Contains the React application's source code.  
- **components/** — Contains reusable React components (Home, Login, Register, Account, Update).  
- **pages/** — Contains different page views (optional for expansion).  
- **App.js** — Root component that defines app routes and structure.  
- **index.js** — Entry point that renders the React app into the DOM.

---

## 🚀 Setup & Installation

### 1️⃣ Clone the Repository
```bash
git clone https://github.com/yourusername/react-auth-app.git
cd react-auth-app
```

### 2️⃣ Install Dependencies
```bash 
npm install
```

### 3️⃣ Start the Development Server
```bash 
npm run dev
```

### 4️⃣ Open in Browser
```bash 
Visit: http://localhost:5173
```

